language: node_js
node_js:
    - "stable"
cache:
    directories:
        - node_modules
before_install:
    - npm install netlify-cli -g
    - npm run test:cov-ci
    - npm run build
    on:
        branch: master